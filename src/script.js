function goBack() {
    window.history.back();
}

function enableInstrumentInput () {
    var isInstrumentChecked = document.getElementById("instrument-check").checked;
    document.getElementById("instrument-input").disabled = !isInstrumentChecked;
    e
}